{
    "description": "Installs a windows feature if its not currently installed.",
    "input_method": "environment",
    "implementations": [
        {"name": "install_windows_feature.ps1", "requirements": ["powershell"]}
      ],
    "parameters": {
      "windows_feature": {
        "description": "Windows feature to install. Must match short name, not display name",
        "type": "String[1]"
      }
    }
  }